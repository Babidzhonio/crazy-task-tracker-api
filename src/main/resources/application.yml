server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost/crazy-task-tracker?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
